<UserControl x:Class="UMLGen.View.UMLClass"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UMLGen.View"
             mc:Ignorable="d" 
             Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}" Width="{Binding Width}" Height="{Binding Height}"
             d:DesignHeight="{Binding Height}" d:DesignWidth="{Binding Width}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="LightBlue" Offset="0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Grid.Row="0">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="WhiteSmoke" Offset="0" />
                </LinearGradientBrush>
            </Grid.Background>
            <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                <TextBlock x:Name="ClassName" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding ClassName}" VerticalAlignment="Top" Height="41" Width="288" Margin="3,3,3,0"/>
            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="WhiteSmoke" Offset="0" />
                </LinearGradientBrush>
            </Grid.Background>
            <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                <TextBlock x:Name="FieldsBox" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding FieldNames}" VerticalAlignment="Top" Height="133" Width="285" Margin="3,3,3,0"/>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="WhiteSmoke" Offset="0" />
                </LinearGradientBrush>
            </Grid.Background>
            <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                <TextBlock x:Name="MethodBox" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding MethodNames}" VerticalAlignment="Top" Height="95" Width="285" Margin="3,3,3,0"/>
            </Border>
        </Grid>

        <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0"></Border>
        <Border BorderBrush="Black" BorderThickness="1" Grid.Row="1"></Border>
        <Border BorderBrush="Black" BorderThickness="1" Grid.Row="2"></Border>

    </Grid>
</UserControl>
