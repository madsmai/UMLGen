<UserControl x:Class="UMLGen.View.UMLClass"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UMLGen.View"
             mc:Ignorable="d" 
             Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}" Width="{Binding Width}" Height="{Binding Height}"
             d:DesignHeight="{Binding Height}" d:DesignWidth="{Binding Width}">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="LightBlue" Offset="0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid HorizontalAlignment="Left" Height="49" VerticalAlignment="Top" Width="auto" Margin="3,3,0,0">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="WhiteSmoke" Offset="0" />
                </LinearGradientBrush>
            </Grid.Background>
            <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                <TextBlock x:Name="ClassName" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding ClassName}" VerticalAlignment="Top" Height="40" Width="190" Margin="3,3,3,0"/>
            </Border>
        </Grid>

        <Grid HorizontalAlignment="Left" Height="140" Margin="3,52,0,0" VerticalAlignment="Top" Width="auto">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="WhiteSmoke" Offset="0" />
                </LinearGradientBrush>
            </Grid.Background>
            <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                <TextBlock x:Name="FieldsBox" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding FieldNames}" VerticalAlignment="Top" Height="100" Width="190" Margin="3,3,3,0"/>
            </Border>
        </Grid>

        <Grid HorizontalAlignment="Left" Height="58" Margin="3,192,0,0" VerticalAlignment="Top" Width="auto">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="WhiteSmoke" Offset="0" />
                </LinearGradientBrush>
            </Grid.Background>
            <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                <TextBlock x:Name="MethodBox" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding MethodNames}" VerticalAlignment="Top" Height="50" Width="190" Margin="3,3,3,0"/>
            </Border>
        </Grid>

        <Border BorderBrush="Black" BorderThickness="3" Grid.Row="0"></Border>

    </Grid>
</UserControl>
